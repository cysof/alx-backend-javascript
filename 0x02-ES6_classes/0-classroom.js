export default class ClassRoom {
  /**
   * Initializes a new instance of the ClassRoom class with the specified maximum number of students.
   *
   * @param {number} maxStudentsSize - The maximum number of students that can be enrolled in the class.
   */
    constructor(maxStudentsSize) {
      this._maxStudentsSize = maxStudentsSize;
    }
  }