#!/usr/bin/node

function getResponseFromAPI() {
    return new Promise(() => {
        // our API call or asynchronous operation goes here
        // once the operation is complete, we resolve the promise
    });

}

export default getResponseFromAPI;